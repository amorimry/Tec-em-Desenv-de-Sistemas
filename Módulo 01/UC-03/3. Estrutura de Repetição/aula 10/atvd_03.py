soma = 0

for i in range(501):
    soma += i
    # isso vai acrescentar
    # usar o += tem que definir antes
    # 0 + 0 = 0; 0 + 1 = 1; 1 + 2 = 3...

print(f"A soma é: {soma}")
# se colocar na identação vai fazer a soma e imprimir, faz um e volta, faz o outro e volta.. e assim vai, meio que vira tipo o histórico